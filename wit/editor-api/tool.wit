package fumo:editor-api@1;

/// Deprecated aggregate world — use [`ffi.wit`](ffi.wit) for the enforceable lowered surface.
///
/// Semantic interfaces live in `core.wit`, `selection.wit`, `assets.wit`, and `scene.wit`.
/// The host contract truth source is `schemas/host-imports.v1.json`.
